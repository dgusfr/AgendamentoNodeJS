var appointment = require("../models/Appointment");
var mongoose = require("mongoose");

class AppointmentService {
  async(name, email, description, cpf, date, time) {}
}

module.exports = new AppointmentService();
